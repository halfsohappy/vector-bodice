"""Pattern Studio — a local GUI for authoring patterns.

Run with:  python -m studio [project.studio.json] [--port N] [--no-browser]

The studio edits a project JSON (measurements, derived values, pieces made
of named points and segments), previews it live through the repo's real
geometry, and exports a ready-to-use pattern folder into patterns/.
"""
